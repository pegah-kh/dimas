# Direct dependencies of xl_vlas, pinned to the versions this repo was
# tested and validated against.
#
# NOT included here: lerobot and LIBERO both need special installation
# section of the README. Install those first following the README, then
# install the rest of these with:
#   pip install -r requirements.txt

einops==0.8.1
gymnasium==1.3.0
numpy==2.2.6
peft==0.19.1
POT==0.9.6.post1
scikit-learn==1.7.1
scipy==1.16.1
termcolor==2.5.0
torch==2.8.0
tqdm==4.67.1
transformers==5.3.0
